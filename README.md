# Koreader-Bookmark-UX-Enhancement
The patch adds extra bookmark and highlight features without replacing full KOReader source modules. It wraps the stock KOReader bookmark, highlight, and annotation behavior so it is easier to keep using after KOReader updates.
